
PREFIX=$(git rev-parse --show-prefix)

for pattern in $@; do
  greps+=" | grep --colour=always $pattern"
done

if [[ -z $PREFIX ]]; then
    eval "git ls-files $greps"
else
    eval "git ls-files $greps" | sed s%`git rev-parse --show-prefix`%%
fi
